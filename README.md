INSTALL LIKE THIS

mkdir -p /tmp/install;cd /tmp/install;wget filenameplaceholder;tar xpf program.tar;mv mainapp.py add_from_repo;chmod +x add_from_repo;mv gui.sh add_from_repo_gui;chmod +x add_from_repo_gui;sudo mv add_from* /usr/local/bin;cd ~;rm -rf /tmp/install
